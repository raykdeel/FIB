#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, m;
    while(cin >> n >> m) {
        vector<pair<int,int>> v(m);
        for(auto& x : v) cin >> x.first >> x.second;
        
    }
}